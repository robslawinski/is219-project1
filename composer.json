{
 "scripts": {
    "post-install-cmd": [
        "chmod -R 777 vendor",
		"chmod -R 777 vendor/jquery",
		"chmod -R 777 vendor/bootstrap",
		"chmod -R 777 vendor/bootstrap/js",
		"chmod -R 777 js",
		"chmod -R 777 css"
    ]
 }
}